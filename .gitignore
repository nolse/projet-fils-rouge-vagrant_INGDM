
# Inventaire généré automatiquement (contient des IPs dynamiques)
inventaire/hosts.yml

# Clés SSH et secrets — ne jamais commiter
.secrets/
*.pem
*.key
inventaire/terraform_ips.json
# Terraform — fichiers generés automatiquement
terraform/.terraform/
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/.terraform.lock.hcl
